@model FileSharingPractice.Models.AddPasswordViewModel 
@inject IViewLocalizer Localize
@{
    ViewData["Title"] = "Add Password";
}
@if (TempData["Success"] != null)
{
    <div class=" alert alert-success text-center">
        <strong>
            @TempData["Success"]
        </strong>
    </div>
}
<section id="mu-contact">
    <div class="container">
        <div class="row">
            <div class="col-md-12">
                <div class="mu-contact-area" style="padding-top:0px">
                    <!-- Title -->
                    <div class="row">
                        <div class="col-md-12">
                            <div class="mu-title">
                                <h2>@Localize["Manage"]</h2>
                            </div>
                        </div>
                    </div>
                    <!-- Start Registration -->
                    <div class="mu-contact-content">
                        <div class="row">
                            <div class="col-md-12">
                                <div class="mu-contact-form-area">
                                    <div id="form-messages"></div>
                                    <form id="contact" method="post" class="mu-contact-form" asp-action="AssPassword" asp-controller="Account">
                                        <div asp-validation-summary="ModelOnly" class="text-danger"></div>
                                        <div class="form-group">
                                            <span class="fa fa-envelope "></span>
                                            <input type="password" asp-for="Password" class="form-control" placeholder=@Localize["CurentPassword"] id="CurentPassword" required>
                                            <span asp-validation-for="Password"></span>
                                        </div>
                                  
                                        <button type="submit" class="mu-send-msg-btn"><span>@Localize["Submit"]</span></button>
                                        
                                    </form>
                                </div>
                            </div>
                        </div>
                    </div>
                    <!-- End registration -->
                </div>
            </div>
        </div>
    </div>
</section>